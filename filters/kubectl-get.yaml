name: "kubectl-get"
version: 1
description: "Basic kubectl output filtering"

match:
  command: "kubectl"

pipeline:
  - action: "strip_ansi"
  - action: "truncate_lines"
    max_length: 200

on_error: "passthrough"
