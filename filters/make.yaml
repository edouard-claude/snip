name: "make"
version: 1
description: "Basic make output filtering"

match:
  command: "make"

pipeline:
  - action: "strip_ansi"
  - action: "truncate_lines"
    max_length: 200

on_error: "passthrough"
