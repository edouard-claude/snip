name: "eslint"
version: 1
description: "Basic eslint output filtering"

match:
  command: "eslint"

pipeline:
  - action: "strip_ansi"
  - action: "truncate_lines"
    max_length: 200

on_error: "passthrough"
