name: "yarn-install"
version: 1
description: "Basic yarn output filtering"

match:
  command: "yarn"

pipeline:
  - action: "strip_ansi"
  - action: "truncate_lines"
    max_length: 200

on_error: "passthrough"
