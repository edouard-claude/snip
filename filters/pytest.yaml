name: "pytest"
version: 1
description: "Basic pytest output filtering"

match:
  command: "pytest"

pipeline:
  - action: "strip_ansi"
  - action: "truncate_lines"
    max_length: 200

on_error: "passthrough"
