name: "pip-install"
version: 1
description: "Basic pip output filtering"

match:
  command: "pip"

pipeline:
  - action: "strip_ansi"
  - action: "truncate_lines"
    max_length: 200

on_error: "passthrough"
