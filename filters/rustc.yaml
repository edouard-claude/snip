name: "rustc"
version: 1
description: "Basic rustc output filtering"

match:
  command: "rustc"

pipeline:
  - action: "strip_ansi"
  - action: "truncate_lines"
    max_length: 200

on_error: "passthrough"
