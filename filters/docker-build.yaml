name: "docker-build"
version: 1
description: "Basic docker output filtering"

match:
  command: "docker"

pipeline:
  - action: "strip_ansi"
  - action: "truncate_lines"
    max_length: 200

on_error: "passthrough"
