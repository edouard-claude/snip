name: "npm-install"
version: 1
description: "Basic npm output filtering"

match:
  command: "npm"

pipeline:
  - action: "strip_ansi"
  - action: "truncate_lines"
    max_length: 200

on_error: "passthrough"
